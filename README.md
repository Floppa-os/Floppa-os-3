# Floppa-os-3
Floppa os 3.1.0
# Floppa os kernel
<p>Floppa os! Специальная ОС на которой можно научиться программировать свои ОС на C/C++ и даже Assembler!</p>
<hr />
<p>Floppa is Kernel развивается сообществом!</p>
<p>&nbsp;</p>
<hr />
<p>Floppa os 3 код представлен ниже</p>
<p><em>Фрагмент кода shell.cpp</em></p>
<hr />
<p></p>
<p>#include &lt;iostream&gt; // Для ввода/вывода (std::cout, std::cin)<br />#include &lt;cstring&gt; // Для функций работы со строками (strcmp, strncmp, strchr)<br />#include "shell.h" // Заголовочный файл для оболочки (предполагаемая декларация функций)<br />#include "../lib/string_utils.h" // Дополнительные утилиты для работы со строками</p>
<p>/**<br /> * Функция обработки команд оболочки<br /> * @param command Указатель на строку с командой<br /> */<br />void execute_command_os(const char* command) {<br /> // Проверка команды "help" &mdash; вывод справки<br /> if (strcmp(command, "help") == 0) {<br /> std::cout &lt;&lt; "Доступные команды:\n";<br /> std::cout &lt;&lt; " help - показать эту справку\n";<br /> std::cout &lt;&lt; " clear - очистить экран\n";<br /> std::cout &lt;&lt; " echo [текст] - вывести текст\n";<br /> std::cout &lt;&lt; " comparatives - показать информацию о степенях сравнения прилагательных\n";<br /> std::cout &lt;&lt; " exit - выйти из оболочки\n";<br /> // Примечание: многократный вывод "FLP#root" выглядит избыточным<br /> for (int i = 0; i &lt; 100; ++i) { // Пример оптимизации (вместо 100 отдельных строк)<br /> std::cout &lt;&lt; " FLP#root\n";<br /> }<br /> }<br /> // Проверка команды "clear" &mdash; очистка экрана<br /> else if (strcmp(command, "clear") == 0) {<br /> std::system("clear"); // Для Unix-систем<br /> // Для Windows: std::system("cls");<br /> }<br /> // Проверка команды "echo" &mdash; вывод текста после пробела<br /> else if (strncmp(command, "echo", 4) == 0) {<br /> const char* space_pos = strchr(command, ' '); // Поиск пробела в строке<br /> if (space_pos) {<br /> std::cout &lt;&lt; (space_pos + 1) &lt;&lt; "\n"; // Вывод текста после пробела<br /> } else {<br /> std::cout &lt;&lt; "\n"; // Если текста нет &mdash; просто перевод строки<br /> }<br /> }<br /> // Проверка команды "comparatives" &mdash; информация о степенях сравнения<br /> else if (strcmp(command, "comparatives") == 0) {<br /> std::cout &lt;&lt; "=== СТЕПЕНИ СРАВНЕНИЯ ПРИЛАГАТЕЛЬНЫХ ===\n\n";<br /> std::cout &lt;&lt; "В английском языке есть 3 степени сравнения:\n";<br /> std::cout &lt;&lt; "- Положительная (исходная форма): big, beautiful, good\n";<br /> std::cout &lt;&lt; "- Сравнительная: bigger, more beautiful, better\n";<br /> std::cout &lt;&lt; "- Превосходная: the biggest, the most beautiful, the best\n\n";</p>
<p>std::cout &lt;&lt; "ПРАВИЛА ОБРАЗОВАНИЯ:\n";<br /> std::cout &lt;&lt; "1. Короткие прилагательные (1&ndash;2 слога):\n";<br /> std::cout &lt;&lt; " - добавляем -er для сравнительной: cold &rarr; colder\n";<br /> std::cout &lt;&lt; " - the + -est для превосходной: cold &rarr; the coldest\n";<br /> std::cout &lt;&lt; "2. Длинные прилагательные (3+ слога):\n";<br /> std::cout &lt;&lt; " - используем 'more' для сравнительной: interesting &rarr; more interesting\n";<br /> std::cout &lt;&lt; " - 'the most' для превосходной: interesting &rarr; the most interesting\n";<br /> std::cout &lt;&lt; "3. Неправильные формы:\n";<br /> std::cout &lt;&lt; " - good &rarr; better &rarr; the best\n";<br /> std::cout &lt;&lt; " - bad &rarr; worse &rarr; the worst\n";<br /> std::cout &lt;&lt; " - far &rarr; farther/further &rarr; the farthest/furthest\n\n";</p>
<p>std::cout &lt;&lt; "ПРИМЕРЫ:\n";<br /> std::cout &lt;&lt; "&bull; small &rarr; smaller &rarr; the smallest\n";<br /> std::cout &lt;&lt; "&bull; happy &rarr; happier &rarr; the happiest\n";<br /> std::cout &lt;&lt; "&bull; modern &rarr; more modern &rarr; the most modern\n";<br /> std::cout &lt;&lt; "&bull; expensive &rarr; more expensive &rarr; the most expensive\n";<br /> }<br /> // Проверка команды "exit" &mdash; завершение работы<br /> else if (strcmp(command, "exit") == 0) {<br /> std::cout &lt;&lt; "Завершение работы оболочки...\n";<br /> }<br /> // Обработка неизвестной команды<br /> else {<br /> std::cout &lt;&lt; "Неизвестная команда: " &lt;&lt; command &lt;&lt; "\n";<br /> std::cout &lt;&lt; "Введите 'help' для справки.\n";<br /> }<br />}</p>
<p>/**<br /> * Функция запуска интерактивной оболочки<br /> */<br />void start_shell_os() {<br /> char input[256]; // Буфер для ввода команды (макс. 256 символов)</p>
<p>while (true) { // Бесконечный цикл до команды "exit"<br /> std::cout &lt;&lt; "FLP#root&gt; "; // Приглашение к вводу<br /> std::cin.getline(input, sizeof(input)); // Чтение строки с консоли</p>
<p>// Проверка, что ввод не пустой<br /> if (strlen(input) &gt; 0) {<br /> execute_command_os(input); // Обработка команды<br /> // Выход из цикла при команде "exit"<br /> if (strcmp(input, "exit") == 0) {<br /> break;<br /> }<br /> }<br /> }<br />}</p>
<p>&nbsp;</p>
